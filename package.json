{
  "name": "mangrove-ocr-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "POC for uploading Thai project documents, calling OCR, and prefilling reviewable structured fields.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "check": "node --check src/worker.js && node --check src/document-schemas.js && node --check src/extract.js && node --check src/confirm.js && node --check public/app-core.js && node --check public/app-flow.js && node --check public/app-review.js",
    "test": "node --test",
    "validate": "npm run check && npm test"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  },
  "engines": {
    "node": ">=22"
  }
}
